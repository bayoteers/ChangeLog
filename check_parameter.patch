diff --git a/editparams.cgi b/editparams.cgi
index a8dc0da..b80e6e8 100755
--- a/editparams.cgi
+++ b/editparams.cgi
@@ -122,6 +122,8 @@ if ($action eq 'save' && $current_module) {
         }
 
         if ($changed) {
+            # Hook to test value
+            Bugzilla::Hook::process('check_parameter', { name => $name, value => $value });
             if (exists $i->{'checker'}) {
                 my $ok = $i->{'checker'}->($value, $i);
                 if ($ok ne "") {
